# A colour STL carries its own

Most STL files carry no colour at all — the plugin falls back to a
theme-aware default. A file in the "Magics" colour format is different: each
facet brings its own colour, and the plugin shows it.

```3d
file: models/colored-octahedron.stl
title: Colour STL
height: 420
```
