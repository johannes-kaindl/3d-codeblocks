# Aiming a camera from the file

`view: camera:<name>` starts from a camera the file itself carries — the angle
whoever built the model considered the right one.

```3d
file: models/doc-camera-floor.gltf
title: Overview
view: camera:Overview
height: 420
```
