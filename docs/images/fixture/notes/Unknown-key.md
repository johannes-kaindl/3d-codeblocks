# A typo should not look like a bug

`heigth:` is a typo for `height:`. The plugin says so below the viewport instead
of silently ignoring the line.

```3d
file: models/ground-floor.gltf
title: Ground floor
heigth: 420
```
